'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import PageShell from '../../components/PageShell';

export default function NewBot(){const router=useRouter();const[language,setLanguage]=useState('NODEJS');const[error,setError]=useState('');const[sending,setSending]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSending(true);setError('');const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/bots',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});if(!r.ok){const b=await r.json().catch(()=>({}));setError(Array.isArray(b.message)?b.message[0]:b.message||'Falha ao criar bot');setSending(false);return}const bot=await r.json();router.push(`/bots/${bot.id}`)}return <PageShell><header><div><small>NOVA APLICAÇÃO</small><h1>Criar bot</h1><p>Escolha o runtime e o arquivo que iniciará sua aplicação.</p></div></header><form className="settingsForm" onSubmit={submit}><label>Nome<input name="name" required minLength={2} maxLength={50} placeholder="Meu bot"/></label><label>Linguagem<select name="language" value={language} onChange={e=>setLanguage(e.target.value)}><option value="NODEJS">Node.js</option><option value="PYTHON">Python</option></select></label><label>Versão<select name="version">{language==='NODEJS'?<><option value="22">Node.js 22</option><option value="20">Node.js 20</option></>:<><option value="3.12">Python 3.12</option><option value="3.11">Python 3.11</option></>}</select></label><label>Imagem<select name="variant"><option value="ALPINE">Alpine — mais leve</option><option value="SLIM">Slim — maior compatibilidade</option></select></label><label>Arquivo inicial<input name="entrypoint" required defaultValue={language==='NODEJS'?'index.js':'main.py'} key={language}/></label>{error&&<div className="authError">{error}</div>}<button disabled={sending}>{sending?'Criando...':'Criar bot'}</button></form></PageShell>}
