'use client';import{useEffect,useState}from'react';import Link from'next/link';import PageShell from'../../components/PageShell';
export default function UsersPage(){const[users,setUsers]=useState<any[]>([]);const load=()=>fetch('/api/admin/users').then(r=>r.ok?r.json():[]).then(setUsers);useEffect(()=>{void load()},[]);async function change(id:string,role:string,status:string){await fetch(`/api/admin/users/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({role,status})});load()}return <PageShell adminOnly><header><div><small>ADMINISTRAÇÃO</small><h1>Usuários</h1><p>Controle acessos, cotas e permissões da plataforma.</p></div></header><div className="tableWrap"><table><thead><tr><th>Usuário</th><th>Bots</th><th>Permissão</th><th>Status</th><th></th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td><b>{u.displayName}</b><small>{u.email}</small></td><td>{u._count.bots}</td><td><select value={u.role} onChange={e=>change(u.id,e.target.value,u.status)}><option value="USER">Usuário</option><option value="ADMIN">Admin</option></select></td><td><select value={u.status} onChange={e=>change(u.id,u.role,e.target.value)}><option value="ACTIVE">Ativo</option><option value="SUSPENDED">Suspenso</option><option value="BANNED">Banido</option></select></td><td><Link className="manage" href={`/admin/users/${u.id}`}>Gerenciar limites</Link></td></tr>)}</tbody></table></div></PageShell>}
