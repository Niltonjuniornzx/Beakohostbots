'use client';import{FormEvent,useEffect,useState}from'react';import{useParams}from'next/navigation';import PageShell from'../../../components/PageShell';
const defaults={maxBots:5,cpuMillicores:250,memoryMb:256,memorySwapMb:256,diskMb:1024,bandwidthIngressMb:10240,bandwidthEgressMb:10240,networkRateKbps:0,pidsLimit:100,maxUploadMb:100,sftpRateKbps:0};
export default function UserDetail(){const{id}=useParams<{id:string}>();const[user,setUser]=useState<any>(null);const[saved,setSaved]=useState(false);const load=()=>fetch(`/api/admin/users/${id}`).then(r=>r.ok?r.json():null).then(setUser);useEffect(()=>{void load()},[id]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaved(false);const raw=Object.fromEntries(new FormData(e.currentTarget));const body:any=Object.fromEntries(Object.entries(raw).map(([k,v])=>[k,Number(v)]));body.cpuMillicores=body.cpuPercent*10;delete body.cpuPercent;const r=await fetch(`/api/admin/users/${id}/limits`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(body)});if(r.ok){setSaved(true);load()}}if(!user)return <PageShell adminOnly><div className="loader"/></PageShell>;const limit={...defaults,...(user.limits[0]||{})};return <PageShell adminOnly><header><div><small>USUÁRIO</small><h1>{user.displayName}</h1><p>{user.email} · {user.role} · {user.status}</p></div></header><form className="limitsForm" onSubmit={save}><h3>Limites individuais</h3><p>Estes valores terão prioridade sobre o padrão global.</p><div className="limitGrid"><label>Máximo de bots<input name="maxBots" type="number" defaultValue={limit.maxBots}/></label><label>CPU por bot (% de 1 vCPU)<input name="cpuPercent" type="number" min="1" max="1000" defaultValue={limit.cpuMillicores/10}/></label><label>RAM por bot (MB)<input name="memoryMb" type="number" defaultValue={limit.memoryMb}/></label><label>RAM + swap (MB)<input name="memorySwapMb" type="number" defaultValue={limit.memorySwapMb}/></label><label>Disco por bot (MB)<input name="diskMb" type="number" defaultValue={limit.diskMb}/></label><label>Download mensal (MB)<input name="bandwidthIngressMb" type="number" defaultValue={limit.bandwidthIngressMb}/></label><label>Upload mensal (MB)<input name="bandwidthEgressMb" type="number" defaultValue={limit.bandwidthEgressMb}/></label><label>Velocidade de rede (Kbps, 0 ilimitado)<input name="networkRateKbps" type="number" defaultValue={limit.networkRateKbps||0}/></label><label>Máximo de processos<input name="pidsLimit" type="number" defaultValue={limit.pidsLimit}/></label><label>Upload de arquivo (MB)<input name="maxUploadMb" type="number" defaultValue={limit.maxUploadMb}/></label><label>Velocidade SFTP (Kbps, 0 ilimitado)<input name="sftpRateKbps" type="number" defaultValue={limit.sftpRateKbps||0}/></label></div>{saved&&<span className="saved">Limites salvos com sucesso.</span>}<button>Salvar limites</button></form><div className="panelTitle"><div><h2>Bots do usuário</h2><p>{user.bots.length} cadastrado(s)</p></div></div><div className="listPanel">{user.bots.map((b:any)=><div className="listRow" key={b.id}><div><b>{b.name}</b><small>{b.runtime.language} {b.runtime.version} · {b.status}</small></div></div>)}</div></PageShell>}
